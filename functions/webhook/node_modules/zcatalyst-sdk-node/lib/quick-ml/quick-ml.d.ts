import { CatalystApp } from 'zcatalyst-sdk-node/lib/catalyst-app';
import { AuthorizedHttpClient } from 'zcatalyst-sdk-node/lib/utils/api-request';
export interface ICatalystQuickMLResponse {
    status: string;
    result: Array<string>;
}
export declare class QuickML {
    requester: AuthorizedHttpClient;
    readonly projectId: string | number;
    constructor(app: CatalystApp);
    predict(endPointKey: string, inputData: Record<string, string>): Promise<ICatalystQuickMLResponse>;
}
